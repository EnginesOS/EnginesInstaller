


# case "$reason" in
#	 BOUND|RENEW|REBIND)
#	   if test $if_up = true
#	    then
#
set >>/tmp/dhcp_trigger_run
echo " "  >>/tmp/dhcp_trigger_run

#want for reason REBOOT and .... 
	        #new_ip=` /opt/engines/bin/get_ip.sh`
	        new_ip=$new_ip_address
	        
	 if ! test -z "$new_ip_address"
	   then        
	         
	       
			if test -f /opt/engines/etc/net/ip
				then
	       			curr_ip=`cat /opt/engines/etc/net/ip`
	    		fi
	    			      
	       if ! test "$curr_ip" = $new_ip
	       	then
	       	sudo /opt/engines/scripts/_refresh_local_hosted_domains.sh $new_ip	       		
	       	fi
	    
   fi
# esac
#	